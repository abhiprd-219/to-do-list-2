*{
    margin:0;
    padding:0;
    box-sizing:border-box ;
    font-weight: bolder;
    font-family: 'Ubuntu', sans-serif;
    outline: none;
    
}
:root{
    --black: indigo;
    --safed: #fff;
    --height: 30px;
}
::selection{
    background-color: var(--safed);
    color: var(--black);
    padding: 3px;
}
body{
    background-color: var(--black);
}
.body{
    var(--height);
    width: 100vw;
    height: 100vh;
    overflow: scroll;
    display: none;
}
.header{
    height:13vh;
    justify-content:center;
    align-items:center ;
    display:flex;
    font-family: 'Ubuntu', sans-serif;
}
h1 {
    color: var(--safed);
}
.main{
    height:13vh;
    justify-content:center;
    align-items:center ;
    display:flex;
}
#note{
    width: 250px;
    height: var(--height);
    border: 4px solid var(--safed);
   /* font-family: 'Kaushan Script', cursive; */
   /* font-family: 'Pangolin', cursive; */
    padding-left: 4px;
    text-transform: capitalize;
}
#add{
    width: 60px;
    height: var(--height);
    background-color: var(--black);
    color: var(--safed);
    border: 2px solid var(--safed);
    border-radius: 0px 20px 20px 0px;
    margin: -4px;
    font-weight: bolder;
    font-size: 16px;
    font-family: 'Pangolin', cursive; 
    
}
.remover {
    margin-left: 5px;
    background-color: var(--black);
    color: red;
    border: none;
    cursor: pointer;
    text-align: right;
}
.list{
    justify-content:center;
    align-items:center ;
    display:flex;
}
li{
    width:70vw;
    padding: 5.0px;
    border:4px solid var(--safed);
    margin: 5px;
    text-align: left;
    background-color: var(--black);
    color: var(--safed);
    list-style: none;
  /*  font-family: 'Ubuntu', sans-serif;  
    font-family: 'Kaushan Script', cursive; */
    font-family: 'Pangolin', cursive; 
    text-transform: capitalize;
}


*{
    padding:0;
    margin:0;
    box-sizing:border-box ;
}
:root{
    --col1: #fff;
    --col2: #000;
}
.loader{
    width:100vw;
    height:100vh;
    background-color: var(--col2);
    justify-content:center;
    align-items:center ;
    display:flex;
    z-index:999999;
    animation:load 3s infinite ;
}
#zamy{
    font-weight:bolder;
    font-size:1.7rem;
    position:fixed;
    font-family: 'Reggae One', cursive;
    bottom:10vh;
    text-align:center;
    animation:load2 3s infinite ;
    padding:10px;
}
#zamy2{
    font-weight:bolder;
    font-size:1.7rem;
    position:fixed;
    font-family: 'Reggae One', cursive;
    top:10vh;
    text-align:center;
    padding:10px;
}

#round{
    width:70px;
    height:70px;
    background-color: var(--col2);
    border:21px solid red;
    justify-content:center;
    align-items:center ;
    display:flex;
    animation:load-rot 3s infinite ;
}
@keyframes load{
    0%{
        background-color: var(--col2);
        border:23px solid var(--col1);
        color: var(--col1);
    }
    50%{
        background-color: var(--col1);
        border:23px solid var(--col2);
        color: var(--col2);
    }
    100%{
        background-color: var(--col2);
        border:23px solid var(--col1);
        color: var(--col1);
    }
    
}
@keyframes load2{
    0%{
        background-color: var(--col2);
        border:5px solid var(--col1);
        color: var(--col1);
        border-radius:1px;
    }
    50%{
        background-color: var(--col1);
        border:5px solid var(--col2);
        color: var(--col2);
        border-radius:30px;
    }
    100%{
        background-color: var(--col2);
        border:5px solid var(--col1);
        color: var(--col1);
        border-radius:1px;
    }
}
@keyframes load-rot{
    0%{
       /*transform:rotate(0deg)*/
        background-color: var(--col2);
        border:21px solid white;
        color: var(--col1);
        transform:scale(0.8);
        border-radius:30%;
    }
    50%{
      /*  transform:rotate(180deg);*/
        background-color: var(--col1);
        border:21px solid black;
        color: var(--col2);
        transform:scale(1.8) rotate(360deg);
        border-radius:0%;
    }
    100%{
       /* transform: rotate(360deg);*/
        background-color: var(--col2);
        border:21px solid white;
        color: var(--col1);
        transform:scale(0.8) , rotate(360deg);
        border-radius:30%;
    
    }
}
